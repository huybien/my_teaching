# Sensitive credentials — never commit
.auto_push/github_info.txt

# Auto-push runtime files
.auto_push/auto_push.log
.auto_push/auto_push.lock

# Reference scripts from another project — not tracked
.meta/

# Office temp/lock files
~$*

# OS artifacts
.DS_Store
Thumbs.db
